# TODO

- [ ] Split doctor.sh into modular checks when: adding a second host, file exceeds ~200 lines, or others need to contribute checks
- [ ] Extract host-specific config (exit node name, etc.) into a config file when adding a second host
